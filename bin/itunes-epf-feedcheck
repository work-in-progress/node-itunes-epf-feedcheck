#!/usr/bin/env node

var argv = require('optimist').argv;
var epf = require('../lib/index');

epf.check(argv.u,argv.p, function(err,data) {
	if(err) {
		console.error( err);
	}	
	else {
		console.log(data);
	}

});

